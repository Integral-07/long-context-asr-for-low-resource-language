# アイヌ語低資源ASR実験 結果まとめ

## 目的

本論文(長文脈Conformer-CTC ASR)のアーキテクチャが低資源言語(アイヌ語)に有用かを検証する。
Karolさんの提案により、以下3つのベースライン実験を同一のデータ分割・評価条件で比較した。

1. 事前学習済みwav2vec2-Ainuを従来手法(短い音声)でファインチューニング
2. 論文アーキテクチャを0から、従来手法(短い音声)で学習
3. 論文アーキテクチャを0から、提案手法(長い音声)で学習

②③はモデル・データソース・トークナイザ・学習レシピを完全に揃え、**文脈長のみを変数**にすることで、
長文脈化単独の効果を切り分けられるように設計した。

## データ

- **コーパス**: サハリンアイヌ「トゥイタハ集」(Tuytah Collection, 浅井タケ氏口承文学、東京外国語大学アーカイブ由来)
- **原資料**: `ainu_corpus/`(全ブランチ共通、`feature/low-resource-finetune` で追加)
  - `ainu_corpus/transcripts/at01.trans.txt`〜`at54.trans.txt`(54ファイル、行単位でローマ字アイヌ語+日本語カタカナ注釈混在)
  - `ainu_corpus/audio/*.wav`(2799クリップ、書き起こし行と1対1対応。発話単位に分割済み)
- **規模**: 53コレクション(at33は音声欠落のため52有効)、合計約8.9時間、コレクション平均約10分(1.7分〜19.2分)
- **train/dev/test分割**: コレクション単位で固定(発話単位でシャッフルすると同一物語・同一話者がtrain/devに漏れるため)。長さで層化し、dev/testにそれぞれ短い〜19分クラスのコレクションを含めた。
  - test: at08, at30, at40, at13, at22
  - dev: at54, at18, at27, at36, at10
  - train: 残り43コレクション
  - この分割は `scripts/ainu_splits.py` / `scripts/prepare_ainu_utterances.py` / `scripts/prepare_ainu.py` で共有し、3実験すべてで同一の held-out コレクションを使用した。

## 実験① wav2vec2-Ainu ファインチューニング

**ブランチ**: `experiment/1-wav2vec2-baseline`

**変更内容**:
- このリポジトリ(lcasr)のコードは使わず、HuggingFace `transformers` による独立パイプラインを追加
- `scripts/prepare_ainu_wav2vec2.py`: 発話単位(書き起こし各行=1サンプル)で `{audio, text}` のtrain/dev/testマニフェストと、train splitのみから構築した文字レベルvocab.json(94トークン、`Wav2Vec2CTCTokenizer`形式)を生成
- `scripts/finetune_wav2vec2_ainu.py`: `karolnowakowski/wav2vec2-large-xlsr-53-pretrain-ain`(234時間のAinu音声で継続事前学習済み、1024次元/24層)をベースに、事前学習専用パラメータ(quantizer等)を破棄し新規CTCヘッドを追加してファインチューニング
- `scripts/eval_wav2vec2_ainu.py`: held-out testセットでの最終評価
- `pyproject.toml` に `transformers`/`accelerate` を追加

**データ**: 発話単位クリップをそのまま使用(連結なし)。30秒を超える外れ値(中央値9.3秒に対し最大96秒)をtrain/devから除外(train 2217件、dev 254件)。testは当初219件で評価し、後に②③と揃えるため209件(30秒以下)に再計算。

**学習設定**: feature encoder(CNN部分)を凍結、バッチサイズ2×勾配累積8、学習率3e-4、30エポック(4170ステップ)、fp16 + gradient checkpointing。

**遭遇した問題と対処**:
- `transformers`未インストール→追加時に副作用で`flash-attn`が消え、再インストールが必要になった
- `transformers` 5.x系でのAPI変更3件(`group_by_length`→`train_sampling_strategy`、`as_target_processor()`廃止など)を修正
- 96秒の外れ値発話がgroup_by_lengthバッチングで同時に載りCUDA OOM→30秒フィルタ+バッチサイズ調整+gradient checkpointingで解消

**結果**:

| 指標 | 値 |
|---|---|
| dev WER | 34.03% |
| test WER(219件, 当初) | 32.31% |
| test WER(209件, ②③と統一後) | **31.47%** |

## 実験② 論文アーキテクチャを0から・短い音声(従来手法)

**ブランチ**: `experiment/2-scratch-short-context`

**変更内容**:
- `scripts/prepare_ainu_utterances.py`: 発話単位(行単位)で lcasr 標準形式(`mapping.json` + `.spec.pt` + 単語タイムスタンプJSON)のtrain/dev/testマニフェストを生成。30秒超の発話を除外するオプションを追加(実験①と条件を統一)
- `scripts/train_ainu_tokenizer.py`: train splitのテキストのみから小型SentencePiece BPEトークナイザ(vocab 500)を新規学習。既存の英語用トークナイザ(4095語彙、Spotify事前学習用)はアイヌ語には使えないため
- `exp/configs/ainu_scratch_short.yaml`: `SCConformerXL`(6層/256次元, 10.1M params)を事前学習なしでランダム初期化から学習。チャンクサイズを3072フレーム(30.72秒)に固定し `sequence_scheduler` を使わないことで、全発話が1チャンクに収まり長文脈機構を一切使わない「従来手法」を実現
- `scripts/eval_ainu_lcasr.py`: lcasrチェックポイントの評価スクリプト(②③共通、pyctcdecode+beam_width=1でのCTCデコードとWER算出)

**データ**: train 2217発話(381.2分)、dev 254発話(38.7分)、test 209発話(34.9分)。

**学習設定**: バッチサイズ32、学習率1e-3(madgrad)、150エポック(332,550レコード相当)、intermediate_loss_weighting 0.5(blank collapse対策)、SpecAugment(5エポック目以降)。

**遭遇した問題と対処**:
- `save_every_n_steps`(累積レコード数単位)を初期値300で走らせたところ、約9ステップごとに保存され40エポック時点で45GB・291ファイルに到達→10000に修正して再実行

**結果**:

| 指標 | 値 |
|---|---|
| train_loss(最終) | 約0.01 |
| dev WER | 58.20% |
| test WER | **58.39%** |

## 実験③ 論文アーキテクチャを0から・長い音声(提案手法)

**ブランチ**: `experiment/3-scratch-long-context`

**変更内容**:
- `scripts/prepare_ainu.py`: 既存の「コレクション連結」スクリプトに、②③共通のtrain/dev/test分割(コレクション単位)を追加(従来は全53コレクションを1つのmapping.jsonにまとめており、dev/testコレクションが学習データに漏れる状態だったため修正)
- `exp/configs/ainu_scratch_long.yaml`: モデル・トークナイザ・学習レシピは実験②と完全に同一。相違点はデータ(連結済み長尺音声)と、`sequence_scheduler` により512フレーム(5.12秒)から16384フレーム(約2.73分、論文の"2.7min"文脈条件に相当)まで段階的にチャンクサイズを伸ばす点のみ
- `scripts/train_ainu_tokenizer.py` / `scripts/eval_ainu_lcasr.py` は実験②からそのまま流用(トークナイザ学習ロジックはテキストのみに依存するため、発話単位/連結版どちらでも同一のトークナイザが得られる)

**データ**: train 43コレクション(7.46時間)、dev 5コレクション(0.75時間)、test 5コレクション(0.70時間)。コレクション内の発話境界(実測)から単語タイムスタンプを線形補間で近似。

**学習設定**: 初期バッチサイズ16(`sequence_scheduler`によりチャンクサイズが伸びるごとに半減)、80エポック(3,440レコード相当)、その他は実験②と同一。

**検証**: 学習前にCPU/GPUスモークテストで、単一の長いコレクション(最大約1000秒)がチャンクをまたいでKVキャッシュを連鎖させながら処理されることを確認(196チャンク処理、エラーなし)。

**結果**:

| 指標 | 値 |
|---|---|
| train_loss(最終) | 約0.01 |
| dev WER | 54.91% |
| test WER | **52.65%** |

GPUメモリ使用量は全期間を通じて1.2GB程度に収まり、24GB GPU(RTX 3090)上でOOMは発生しなかった。

## 追加検証: ②③のk-fold交差検証

**ブランチ**: `experiment/4-kfold-cv`

**動機**: ②③の比較はheld-outが各5コレクションしかなく、厳密符号検定の理論上最良のp値でも0.0625までしか下がらない(5コレクション全部で③が勝っても、慣習的な有意水準0.05を原理的に超えられない)。この標本数の壁を解消するため、全53コレクションをheld-outとして使うk-fold交差検証に切り替えた。

**変更内容**:
- `scripts/ainu_kfold_splits.py`: 53コレクション(at33は書き起こし空のため除外)を、実測音声長からLPTヒューリスティックで5foldに分割(各fold 1.75〜1.80時間、10〜11コレクション)。fold番号はコレクション単位で固定し、②(発話単位)と③(コレクション連結)が常に同じfoldに割り当たることを保証(片方のtrainがもう片方のtestに漏れるリークを防止)
- `prepare_ainu.py` / `prepare_ainu_utterances.py` / `train_ainu_tokenizer.py` に `--fold` オプションを追加。指定時は固定split(train43/dev5/test5)の代わりにfold単位のtrain/test分割を使う(devは使わない — このコードベースの学習ループはdevをチェックポイント選択等に使っておらず、実質testと役割が重複するだけだったため)
- `generate_kfold_configs.py`: 既存の`ainu_scratch_{short,long}.yaml`を基に、fold毎の学習config(計10個)を自動生成。モデル・学習レシピは一切変更せず、`data.path`/`checkpointing.dir`/`tokenizer_path`のみ差し替え
- `run_ainu_kfold.sh` / `aggregate_kfold_results.py`: 各foldのデータ準備→トークナイザ学習→②③学習→②③評価を通して実行し、全foldのtest_predictions.jsonをコレクション単位に集約してペア化ブートストラップ+厳密符号検定を行う

**学習設定**: モデル・学習レシピは元の実験②③と完全同一(`SCConformerXL`, 10.1Mパラメータ)。5fold×2条件=計10回のフル学習を`dl12`(RTX 3090)上で実行。

**結果**(全53コレクションを1回ずつheld-outとして評価し、プールしたcorpus-level WER):

| | WER |
|---|---|
| ② scratch-short | 57.37% |
| ③ scratch-long | 54.67% |
| 差分 | **-2.71pt** |

- 95% CI(コレクション単位ペア化ブートストラップ): [-3.81pt, -1.53pt]
- 厳密符号検定: **41/53コレクションで③が改善、p = 0.0001**(単一split時の理論上限p=0.0625を大きく下回り、有意水準0.05を明確にクリア)

fold別内訳:

| fold | ② short | ③ long | 差分 |
|---|---|---|---|
| 0 | 58.07% | 52.64% | -5.43pt |
| 1 | 56.07% | 54.96% | -1.11pt |
| 2 | 55.35% | 55.69% | +0.34pt(唯一の逆転、誤差範囲) |
| 3 | 56.31% | 52.63% | -3.68pt |
| 4 | 61.65% | 57.76% | -3.89pt |

**遭遇した問題と対処**:
- `dl12`の`--extra cpu`環境がtorchaudioのバイナリ不整合(`undefined symbol: aoti_torch_abi_version`)で壊れていたため、データ準備を含む全ステップを`--extra gpu`に統一
- パイプライン最後の集計コマンドが素の`python`を呼んでおり、ホストにそのバイナリが無く失敗(学習・評価自体は完走済みだったため、予測ファイルを取得して手元で集計。後日`uv run --extra gpu python`に修正しコミット)
- SSH監視接続がネットワーク瞬断で複数回切断されたが、学習プロセス自体は`nohup`で保護されており無停止で完走(10run・約6時間)

**結論**: 単一split(n=5)では「一貫した方向」までしか言えず統計的有意性は主張できなかった長文脈化の効果が、k-fold CV(n=53)により**有意水準0.05を明確にクリア(p=0.0001)**した。効果量は単一splitの-5.7ptよりやや小さい-2.71ptだが、これは全コレクションを均等にカバーしたより信頼できる推定値であり、単一splitの数字はたまたま効果が大きく出やすい5コレクションを引いていた可能性を示唆する。

## 実験⑤ wav2vec2-Ainu(事前学習あり)+ 長文脈シーケンス長ウォームアップ

**ブランチ**: `experiment/5-wav2vec2-longcontext`(`experiment/4-kfold-cv`の分岐元 78a89c8 から兄弟ブランチとして作成)

**動機**: 実験①(事前学習の効果、約20〜27pt)と②→③(長文脈化の効果、-3.3〜-5.7pt)は別々に確認できたが、両方を組み合わせたときにどうなるかは未検証だった。karolのwav2vec2-Ainu(事前学習済みCNN+24層Transformer)に、②③で使った「学習サンプルとして見せる音声の長さを段階的に伸ばす」カリキュラム(sequence_scheduler)を組み合わせるハイブリッド構成を実装した。

**設計上の主な決定**(詳細検討の経緯は省略):
- 論文のSCConformerXLは実際にはチャンク跨ぎのKVキャッシュを使っていない(`sconformer_xl.py`に不使用を示すコメントあり、`cached_kvs`は常に`None`)ことが判明。「長文脈化」の正体は「1つの独立した学習サンプルとして見せる音声の長さ」を伸ばすカリキュラムだった。これによりKVキャッシュ・RoPE・チャンク境界のオーバーラップ処理の追加実装は不要と判断
- `lcasr/models/wav2vec2_longcontext.py`: 新モデルクラス`Wav2Vec2CTCLongContext`。CNN feature extractorは含めず(前処理スクリプトで凍結・事前計算)、`feature_projection`以降のTransformer(24層、事前学習済み)と`ASRLinearSCDecoder`のみを保持。self-conditioning(②③のblank collapse対策)を踏襲
- `scripts/prepare_ainu_wav2vec2_{longcontext,utterances}.py`: コレクション連結版(条件B、長文脈)と発話単位版(条件A、短文脈)。CNN部分は凍結してコレクション単位で事前計算・キャッシュし、学習ループでは再計算しない
- `exp/configs/ainu_wav2vec2_{short,long}.yaml`: 条件A/Bで文脈長関連の設定(`audio_chunking`/`sequence_scheduler`)以外は完全に同一(②③の比較設計を踏襲)

**遭遇した問題と対処**:
- コレクション全体(最長19分超)をfloat32で一括CNNに通すとGPUメモリを使い切りOOM(dl12実機で確認)。fp16実行に切り替えてメモリを半減させ、それでも足りない収録だけCPUにフォールバックする方式に変更
- `transformers`5.x系(dl12実機の実際の環境)ではSDPA用のAttention maskの扱いが変わっており、手組みのマスクが形状不一致でクラッシュ。HF純正の`transformers.masking_utils.create_bidirectional_mask`に差し替え。同バージョンでは各層のforwardがタプルでなくTensorを直接返すため、インデックス`[0]`がバッチ次元を破壊するサイレントなバグも発見・修正
- **`chunk_text_json`(音声チャンクの時間窓を秒に変換する共有関数)がmel-spectrogram用のhop=160(100Hz)をハードコードしたままだった**。wav2vec2特徴量は実際には320サンプル/フレーム(50Hz)なので、チャンクの時間窓を半分の長さと誤認識し、特にチャンク数の多い長い収録(条件B)で音声とテキストの対応が完全に崩れ、test WER=100%(全予測が空)のblank collapseになった。学習loss自体は終始安定していたため訓練の発散では見抜けず、評価まで進めて初めて発覚した。`hop_length`引数を追加(デフォルトは既存のまま、他の呼び出し元には影響なし)し、config経由で320を渡すよう修正
- 自己条件付け(self-conditioning、全24層で中間CTC loss+decoder出力の再投入)を無効化するアブレーションを試したところ、lossは滑らかに下がったが評価はtest WER=100%(別種の崩壊)になった。訓練ログのデコード例が高頻度語(「manu」等)の反復になっており、見かけ上の低lossはCTCが反復パターンで部分的に整合を取れてしまうことによる偽物と判明。自己条件付きの版は訓練が不安定(epoch8が最良、epoch12-20で悪化、epoch30で回復)だったが実際の予測文は崩壊していなかったため、自己条件付けは有効(true)のまま採用した

**データ**: 条件A(発話単位)train 2217/dev 254/test 209発話、条件B(コレクション連結)train 43/dev 5/test 5コレクション(実験②③と同一split)。

**学習設定**: 両条件ともbatch_size 8(A)/4(B, sequence_schedulerで縮小)、AdamW lr=3e-4、30エポック、self_conditioning有効(intermediate_loss_weighting 0.5)。条件Bのみ`sequence_scheduler`で256フレーム(5.12秒)から8192フレーム(約2.73分、実験③と同じ最大文脈長)まで段階的に伸長。

**結果**(単一split、30エポック完走):

| 指標 | 条件A(短文脈) | 条件B(長文脈) |
|---|---|---|
| test WER | 60.65% | **58.32%** |

条件Bが条件Aを**-2.33pt**上回った。②→③の効果(-5.7pt)より小さいが方向は一致している。

**限界**: 単一split(n=5)のため②③同様に統計的有意性は未確認(k-fold検証は今後の展望参照)。絶対値(58〜61%)は実験①(31.47%)よりかなり悪く、学習レシピ(自己条件付けの訓練不安定さ、学習率など)にまだ改善の余地がある。

## 結果比較

| # | 内容 | モデル規模 | dev WER | test WER |
|---|---|---|---|---|
| ① | wav2vec2-Ainu(事前学習あり)+ 従来手法 | 315M | 34.03% | 31.47% |
| ② | 論文アーキテクチャ・0から + 従来手法(短い音声) | 10M | 58.20% | 58.39% |
| ③ | 論文アーキテクチャ・0から + 提案手法(長い音声) | 10M | 54.91% | **52.65%** |
| ⑤A | wav2vec2-Ainu(事前学習あり)+ 従来手法(単一split再検証) | 315M | - | 60.65% |
| ⑤B | wav2vec2-Ainu(事前学習あり)+ 提案手法(長い音声) | 315M | - | 58.32% |

②→③の差分(モデル・データソース・トークナイザ・学習レシピを固定し、文脈長のみを変えた結果):

- dev: 58.20% → 54.91%(**-3.3pt**)
- test: 58.39% → 52.65%(**-5.7pt**)

## 何が言えるか

1. **長文脈化は一貫してWERを改善した**(②→③でdev/testとも改善)。他の条件をすべて固定した上での比較なので、この改善は長文脈機構そのものに帰属できる。論文の中心的主張(文脈を伸ばすとASR精度が上がる)は、このアイヌ語コーパスでも(小規模ながら)再現された。単一split(n=5)の時点では統計的有意性まで主張できなかったが、後述のk-fold交差検証(n=53)でp=0.0001を確認しており、この改善は統計的にも裏付けられている。
2. ただし**改善幅(-3.3〜-5.7pt)は、事前学習の効果(①と②③の差、約20〜27pt)に比べると小さい**。この低資源設定(train 7.5時間)では、長文脈アーキテクチャ単体よりも大規模事前学習(234時間, 315Mパラメータ)の方が支配的な要因である。
3. したがって「論文のアーキテクチャは低資源言語に有用か」という問いへの答えは、**限定的だが一貫したプラス効果がある**、という中間的な結論になる。長文脈機構は無視できない改善をもたらすが、それ単独で事前学習の欠如を補うには至らない。
4. 実験①のtest WER(31.47%)が①②③の中で最良である点は、実運用でアイヌ語ASRを構築する場合、まず大規模事前学習済みモデル(wav2vec2-Ainu等)を出発点にすべきことを示唆する。長文脈化の効果を、その上に「追加で」乗せた場合にどうなるかは実験⑤(後述)で検証した——単一splitでは-2.33ptの改善が見られたが、絶対WERは実験①にまだ及ばず、k-fold検証・レシピ改善が今後の課題として残る。

## 限界

- 単一split時点でのdev/testの規模が小さい(実験③はコレクション単位で各5件、実験②は発話単位で200件強)問題は、k-fold交差検証(n=53、上記参照)で解消した。ただし乱数シード(モデル初期化)を変えた複数run平均はまだ未実施——k-foldは分割を変えているだけで、同一splitでのシード間分散は検証していない。
- ハイパーパラメータ探索は最小限(スモークテスト1回+本走行1回)。学習率・モデルサイズ・vocab_sizeなどは論文の設定や経験則からの一点決め打ち。
- 実験③の文脈長は最大2.73分までしか伸ばしていない(論文が報告する20分条件は未検証)。GPUメモリ(24GB, RTX 3090)とデータ規模(コレクション平均10分)の制約による。
- 実験①のtest評価は当初219件(未フィルタ)で行い、後から209件(30秒以下)に絞って再計算した経緯があるため、レポートする際はどちらの数値かを明記する必要がある(本ドキュメントでは統一後の209件版を採用)。

## 今後の展望

1. **実験⑤のk-fold交差検証(進行中)**: 実験⑤(単一split、n=5)で条件B(長文脈)が条件Aを-2.33pt上回ったが、②③同様に統計的有意性は未確認。`ainu_kfold_splits.py`を共有し、実験④と同じ手法(5fold×2条件=10回のフル学習、コレクション単位ペア化ブートストラップ+符号検定)で検証する。
2. **実験⑤のレシピ改善**: 絶対WER(58〜61%)が実験①(31.47%)よりかなり悪い。自己条件付きでの訓練不安定さ(epoch12-20での悪化)の原因(学習率、self-conditioningの重み等)を追加調査する余地がある。
3. **文脈長のスケーリング検証**: 実験③の2.73分よりさらに長い文脈(論文の20分条件相当)で試し、改善が頭打ちになるか、さらに伸びるかを確認する。
3. **複数シードでの再実行**(一部完了): k-fold交差検証によりsplitを変えた頑健性は確認済み(p=0.0001)。残るは同一splitでモデル初期化シードだけを変えた複数runでの再現性確認。
4. **ハイパーパラメータ探索**: 学習率・vocab_size・モデルサイズについて簡単なスイープを行い、③の結果が最適点に近いか確認する。
5. **エラー分析**: 実験②③のtest予測(`checkpoints/ainu_scratch_short/test_predictions.json`, `checkpoints/ainu_scratch_long/test_predictions.json`)を用いて、長文脈化がどのような誤り(挿入/削除/置換、特定の語彙)を減らしているのか定性分析する。
